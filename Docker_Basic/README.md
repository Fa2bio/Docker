<h1>Docker-Basic</h1>

### Install Docker
```xml
pamac install docker
```

### Start Docker Service
```xml
sudo systemctl start docker
```

### Stop Docker Service
```xml
sudo systemctl stop docker
```